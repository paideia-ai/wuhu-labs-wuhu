{
  "workspace": [
    "./packages/sandbox-daemon",
    "./packages/server",
    "./packages/prisma"
  ],
  "fmt": {
    "indentWidth": 2,
    "semiColons": false,
    "singleQuote": true,
    "exclude": [
      "packages/prisma/generated/"
    ]
  },
  "lint": {
    "include": [
      "packages/"
    ],
    "exclude": [
      "packages/prisma/generated/"
    ],
    "rules": {
      "exclude": [
        "require-await",
        "no-slow-types",
        "no-window"
      ]
    }
  },
  "tasks": {
    "check": "deno task --cwd=packages/sandbox-daemon check",
    "test": "deno task --cwd=packages/sandbox-daemon test",
    "verify": "deno task -r check && deno task -r test",
    "coverage": "deno task --cwd=packages/sandbox-daemon coverage",
    "coverage:check": "deno task --cwd=packages/sandbox-daemon coverage:check",
    "prisma:gen": "cd prisma && npx prisma generate",
    "prisma:push": "cd prisma && npx prisma db push"
  },
  "imports": {
    "@std/path": "jsr:@std/path@^1"
  }
}
